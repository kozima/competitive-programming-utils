# -*- mode: snippet -*-
# name: read-digraph
# key: read-digraph
# --
vector<vector<int> > adj(n);
for (int i = 0; i < m; i++) {
    int u, v; cin >> u >> v;
    u--; v--;
    adj[u].push_back(v);
}
$0